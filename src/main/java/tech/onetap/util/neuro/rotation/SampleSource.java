package tech.onetap.util.neuro.rotation;

/**
 * Источник метки. Смешивать источники в одном обучении нельзя без явного разрешения.
 */
public enum SampleSource {
    /** Метка — фактическая дельта мыши человека. */
    HUMAN,
    /** Метка — дельта, выданная детерминированным режимом ротации. */
    ROTATION
}
